name: company_commute
description: An app displaying all the company&#x27;s employees&#x27; commutes
version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  firebase: ^5.0.0
  http: ^0.12.0+2
  google_maps_flutter: ^0.5.21
  url_launcher: ^5.1.2
  firebase_core: ^0.4.0+1
  web_socket_channel: ^1.0.15

  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/marker_adorsys_big.png
    - assets/marker_adorsys_small.png
    - assets/adorsys_centered.png